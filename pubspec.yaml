name: hilpad
description: Class Management for HiLCoE students by HiLCoE students.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  get:
  get_storage:
  bottom_navy_bar:
  dio: ^4.0.6
  dio_http_cache: ^0.3.0
  intl: ^0.17.0
  path_provider: ^2.0.11
  open_file: ^3.2.1
  flutter_signin_button: ^2.0.0
  timelines: ^0.1.0
  awesome_select: ^6.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true


  assets:
     - assets/images/logo.png

